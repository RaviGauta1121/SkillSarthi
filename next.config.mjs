/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'lh3.googleusercontent.com', 
      'avatars.githubusercontent.com', 
      'assets.aceternity.com'  // Add the new domain here
    ],
  },
};

export default nextConfig;
